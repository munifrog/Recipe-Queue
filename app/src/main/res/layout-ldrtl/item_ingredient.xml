<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:focusable="true"
    android:nextFocusRight="@+id/iv_favorite"
    android:nextFocusLeft="@+id/btn_send_to_list"
    android:nextFocusUp="@+id/iv_favorite"
    android:nextFocusDown="@id/btn_send_to_list"
    android:orientation="vertical" >

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >

        <View
            android:id="@+id/iv_focus_scrim"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/context_focus_scrim"
            android:background="@color/transparent" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal" >

            <ImageView
                android:id="@+id/iv_ingredient_image"
                android:layout_width="@dimen/width_icon_ingredient"
                android:layout_height="@dimen/height_icon_ingredient"
                android:layout_gravity="center"
                android:contentDescription="@string/ingredient_description" />

            <TextView
                android:id="@+id/iv_ingredient_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_margin="@dimen/margin_item_ingredient"
                android:textSize="@dimen/text_size_recipe_ingredients_item"
                android:textColor="@color/scrim_text"
                tools:text="Carrots" />

            <TextView
                android:id="@+id/iv_ingredient_amount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_margin="@dimen/margin_item_ingredient"
                android:textSize="@dimen/text_size_recipe_ingredients_item"
                android:textColor="@color/scrim_text"
                tools:text="1.0 large" />

        </LinearLayout>

    </FrameLayout>

    <View style="@style/DividerHorizontal" android:background="@color/scrim_text" />

</LinearLayout>